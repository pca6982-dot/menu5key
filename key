local CurrentKey = MachoAuthenticationKey(272100544502608783)

if not string.find(KeysBin, CurrentKey, 1, true) then
    MachoMenuNotification("Authentication Failed", "Your key is not authorized.")
    return
end
